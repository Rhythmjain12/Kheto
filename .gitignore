# ── Node / Firebase Functions ─────────────────────────────────────────────────
node_modules/
firebase-debug.log
.firebase/

# ── Environment / Secrets ─────────────────────────────────────────────────────
.env
.env.local
*.env
functions/.env

# ── Flutter app secrets ────────────────────────────────────────────────────────
# api_keys.dart contains live API keys — never commit it.
# Use api_keys.dart.example as the committed template.
agroshield/lib/config/api_keys.dart

# ── Android secrets ───────────────────────────────────────────────────────────
agroshield/android/app/google-services.json
agroshield/android/app/*.keystore
agroshield/android/local.properties
agroshield/android/key.properties
agroshield/android/.gradle

# ── iOS secrets ───────────────────────────────────────────────────────────────
agroshield/ios/GoogleService-Info.plist
agroshield/ios/Flutter/flutter_export_environment.sh

# ── Flutter build artifacts ───────────────────────────────────────────────────
agroshield/.dart_tool/
agroshield/.flutter-plugins
agroshield/.flutter-plugins-dependencies
agroshield/.packages
agroshield/build/
agroshield/.pub-cache/
agroshield/.pub/
agroshield/*.iml
agroshield/*.ipr
agroshield/*.iws
agroshield/.idea/

# ── Internal dev / AI session artifacts ──────────────────────────────────────
# These are private working notes and AI artifacts — not part of the product.
HANDOFF_*.md
Handoffs/
PROJECT_MASTER.md
PLAY_STORE_TASKS.md
LINKEDIN_POSTS.md
Claude artifacts/
Google stitch front end pages/
cleanup_old_fires.js
qa_firebase.js
qa_score_engine.js
screenshots/
!docs/screenshots/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
**/.DS_Store
WRAP_UP_PROMPT.md
.claude/

# Untracked images at repo root (one-off AI exports, duplicate logos)
Gemini_Generated_Image_*.png
khetogreen_*.png

# Source folder for Play Store screenshots (large jpgs already mirrored to docs/screenshots/)
agroshield/assets/PlayStore images/
